# Auth now runs fully in the browser on a SQLite (sql.js) database persisted in
# localStorage — no external service or credentials required. This file is kept
# as a placeholder for any future client-side env vars.

# Production domain Caddy serves on (docker-compose.yml). Only needed when you
# run the production stack with Caddy/HTTPS; leave blank for local dev.
DOMAIN=

# Note: Twitch credentials for channel-specific sub/bit badges are NOT set here —
# enter them in the Admin dashboard (stored server-side in the DB).
